var fs = require('fs')

var Browser = require('zombie')
var zombie = new Browser()

var zombiecss = require('../src/zombiecss')

var afterUrl = 'file:///Users/prestl02/Projects/zombiecss/sample/after.html'

zombie
    .visit(afterUrl)
    .then(function() {
        zombiecss.compare(zombie, {
            baseDir: './snapshots',
            expectedFile: 'sample_before',
            actualFile: 'sample_after',
            diffFile: 'sample_diff',
            selector: '#sample',
            reportLocation: './report.html'
        })
    })
